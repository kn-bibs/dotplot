language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"

install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov coveralls


script:
  - python3 -m pytest
  - python3 -m pytest --cov=.

after_success:
    coveralls


